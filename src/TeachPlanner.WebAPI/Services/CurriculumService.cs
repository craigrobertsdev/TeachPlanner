namespace TeachPlanner.Api.Services;
public class CurriculumService
{
    // this class will be loaded as a singleton by the DI container
    // and will contain a list of all curriculum subjects for access by
    // every teacher
}
